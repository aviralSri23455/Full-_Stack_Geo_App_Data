import L from 'leaflet';

export const defaultCenter = [51.505, -0.09];
export const defaultZoom = 13;

export const tileLayerUrl = 'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png';
export const tileLayerOptions = {
  maxZoom: 18,
};
